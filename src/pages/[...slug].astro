---
import type { CollectionEntry } from 'astro:content';
type Journal = CollectionEntry<'journal'>;
import { getCollection, render } from 'astro:content';

import { site } from '@/data/site';

import { formatPosts, sortByDate, dateToFormat, dateToISO } from '@/utils';

import { mapping } from '@/mdx-components';

import BaseLayout from '@/layouts/BaseLayout.astro';
import ListItem from '@/components/ui/ListItem';
import OrderedList from '@/components/ui/OrderedList';
import PageTitle from '@/components/site/PageTitle.astro';
import SectionLabel from '@/components/ui/SectionLabel';
import Tag from '@/components/ui/Tag';
import Text from '@/components/ui/Text';
import TextLink from '@/components/ui/TextLink';

export async function getStaticPaths() {
	const journalEntries = await getCollection('journal', ({ data }: Journal) => !data.draft);
	const numberOfPages = journalEntries.length;
	const formattedJournalEntries: Journal[] = formatPosts(journalEntries, { sortOrder: 'asc' });

	return formattedJournalEntries.map((entry, index) => ({
		params: { slug: entry.id },
		props: {
			entry,
			next:
				index + 1 === numberOfPages
					? { id: null, data: null }
					: formattedJournalEntries[index + 1],
			prev: index === 0 ? { id: null, data: null } : formattedJournalEntries[index - 1],
		},
	}));
}

const { entry, prev, next } = Astro.props;
const entryId = entry.id;
const {
	Content,
	remarkPluginFrontmatter: { minutesRead },
} = await render(entry);

const seriesEntries: Journal[] = await getCollection('journal', ({ data }: Journal) => {
	return data.series === entry.data.series && !data.draft;
});

seriesEntries.sort(sortByDate).reverse();

const title = entry.data.title;
const subtitle = entry.data.subtitle;
const description = entry.data.description;

const schema = JSON.stringify({
	'@context': 'https://schema.org',
	'@type': 'Article',
	headline: entry.data.title,
	description: entry.data.description,
	datePublished: entry.data.date,
	dateModified: entry.data.updated,
	image: [
		entry.data.cover ? site.url + entry.data.cover : `${site.url}/assets/images/og/bonsai.jpg`,
	],
	author: [
		{
			'@type': 'Person',
			name: entry.data.author || site.author,
			url: `${site.url}/about`,
		},
	],
	mainEntityOfPage: {
		'@type': 'webPage',
		id: `${site.url}/${entryId}/`,
	},
});
---

<BaseLayout
	cover={entry.data.cover}
	description={description}
	nofollow={entry.data.nofollow}
	noindex={entry.data.noindex}
	title={subtitle ? `${title}: ${subtitle}` : title}
>
	<PageTitle class="pb-4 text-[clamp(3rem,6vw,7rem)] italic leading-[0.92]">{title}</PageTitle>
	{
		subtitle && (
			<div class="max-w-360 mx-auto w-full px-8 pb-8">
				<Text className="text-hai italic mbe-0">{subtitle}</Text>
			</div>
		)
	}

	<!-- Cover image -->
	{
		entry.data.cover && (
			<div class="max-w-360 mx-auto w-full px-8 pb-12">
				<img
					src={entry.data.cover}
					alt={title}
					class="rounded-2 w-full object-cover"
					loading="eager"
					decoding="async"
				/>
			</div>
		)
	}

	<!-- Article layout: meta + content -->
	<div class="max-w-360 mx-auto w-full px-8 pb-32">
		<div class="grid gap-6 md:grid-cols-[160px_1fr] md:gap-16">
			<!-- Meta column -->
			<aside>
				<div class="text-2 text-hai sticky top-8 flex flex-col gap-4">
					<div>
						<time datetime={dateToISO(entry.data.date)}>
							{dateToFormat(entry.data.date)}
						</time>
					</div>
					<div>
						{Math.round(minutesRead)} min read
					</div>
					{
						entry.data.tags && entry.data.tags.length > 0 && (
							<div class="mt-2 flex flex-wrap gap-2">
								{entry.data.tags
									.sort((a: string, b: string) => a.localeCompare(b))
									.map((t: string) => (
										<Tag href={`/writing/?tag=${t}`}>{t}</Tag>
									))}
							</div>
						)
					}
				</div>
			</aside>

			<!-- Article body -->
			<article class="journal-post min-w-0">
				{
					entry.data.series && seriesEntries.length > 1 && (
						<div class="rounded-2 border-shibui-200 dark:border-shibui-800 mb-8 border p-4">
							<SectionLabel as="p" className="mbe-3">
								Series
							</SectionLabel>
							<OrderedList className="flex flex-col gap-1 mbe-0 pis-0">
								{seriesEntries.map((item) => (
									<ListItem className="mbe-1">
										{entryId === item.id ? (
											<em class="text-sumi dark:text-washi font-bold">
												{item.data.title}
											</em>
										) : (
											<TextLink href={`/${item.id}/`}>
												{item.data.title}
											</TextLink>
										)}
									</ListItem>
								))}
							</OrderedList>
						</div>
					)
				}
				<Content components={mapping} />
				{
					entry.data.updated && (
						<footer class="text-2 text-hai mt-12">
							<span class="font-bold">Last Updated:</span>{' '}
							<time datetime={dateToISO(entry.data.updated)}>
								{dateToFormat(entry.data.updated)}
							</time>
						</footer>
					)
				}
			</article>
		</div>
	</div>

	<!-- Prev/Next navigation -->
	{
		(prev.id || next.id) && (
			<nav class="border-shibui-200 dark:border-shibui-800 max-w-360 mx-auto grid w-full grid-cols-2 gap-8 border-t px-8 pb-16 pt-8">
				<div>
					{prev.id && (
						<a
							class="group flex flex-col gap-1 transition-opacity hover:opacity-60"
							href={`/${prev.id}/`}
						>
							<span class="text-2 text-hai uppercase tracking-widest">Previous</span>
							<span class="text-3 text-sumi dark:text-washi font-bold">
								{prev.data.title}
							</span>
						</a>
					)}
				</div>
				<div class="text-right">
					{next.id && (
						<a
							class="group flex flex-col gap-1 transition-opacity hover:opacity-60"
							href={`/${next.id}/`}
						>
							<span class="text-2 text-hai uppercase tracking-widest">Next</span>
							<span class="text-3 text-sumi dark:text-washi font-bold">
								{next.data.title}
							</span>
						</a>
					)}
				</div>
			</nav>
		)
	}
</BaseLayout>

<script is:inline type="application/ld+json" set:html={schema} />

<style is:global>
	@reference "../styles/global.css";

	.journal-post {
		& p {
			@apply text-3 mb-5 leading-relaxed;
		}

		& h2 {
			@apply font-display text-5 text-sumi dark:text-washi mb-4 mt-16 font-bold tracking-tight;
		}

		& h3 {
			@apply text-3 text-sumi dark:text-washi mb-2 mt-10 font-bold;
		}

		& h4,
		& h5,
		& h6 {
			@apply text-3 text-sumi dark:text-washi mb-2 mt-8 font-bold;
		}

		& ul,
		& ol {
			@apply text-3 mb-5 leading-relaxed;
		}

		& ul {
			@apply list-disc pl-5 md:pl-0;
		}

		& ol {
			@apply list-decimal pl-5 md:pl-0;
		}

		& li {
			@apply mb-1;
		}

		& a {
			@apply text-beni underline transition-opacity hover:opacity-60;
		}

		& strong {
			@apply font-bold;
		}

		& em {
			@apply italic;
		}

		& hr {
			@apply border-shibui-300 dark:border-shibui-700 my-12;
		}

		& blockquote {
			@apply border-beni text-hai mb-5 border-l-2 pl-4 italic;
		}

		& pre {
			@apply rounded-2 text-code mb-8 overflow-auto p-4 font-mono;
		}

		& code:not(pre code) {
			@apply text-code bg-shibui-200 dark:bg-shibui-800 rounded px-1 font-mono;
		}

		& figure {
			@apply mb-8;
		}

		& img {
			@apply rounded-2 w-full;
		}

		/** Footnotes */
		.footnotes {
			@apply border-shibui-200 dark:border-shibui-800 mt-12 border-t pt-8;
		}

		.footnotes ol {
			@apply text-2 text-hai flex list-none flex-col gap-2 p-0;
		}

		.footnotes li {
			@apply flex gap-2;
		}

		.footnotes p {
			@apply text-2! text-hai! mb-0!;
		}

		.footnotes a {
			@apply text-beni!;
		}

		[data-footnote-ref] {
			@apply text-beni! relative ms-[0.2em] font-light not-italic no-underline;
		}
	}
</style>
