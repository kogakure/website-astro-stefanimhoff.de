---
import WorkImage from './WorkImage';

interface Props {
	src: string;
	alt: string;
	sizes: string;
	className?: string;
}

const { src, alt, className } = Astro.props;
---

<WorkImage src={src} largeSrc={src} alt={alt} className={className} client:visible />
