---
import '../styles/global.css';
import { ClientRouter } from 'astro:transitions';
import { SEO } from 'astro-seo';
import { site } from '../data/site';
import { isProduction } from '../utils';

import ThemeProvider from '../components/site/ThemeProvider.astro';
import PageHeader from '../components/site/PageHeader.astro';
import PageFooter from '../components/site/PageFooter.astro';
import CommandMenu from '../components/interactive/CommandMenu';
import HoverPreview from '../components/interactive/HoverPreview';
import Scripts from '../components/site/Scripts.astro';

export interface Props {
	cover?: string;
	description?: string;
	footer?: boolean;
	header?: boolean;
	nofollow?: boolean;
	noindex?: boolean;
	title: string;
}

const {
	description,
	footer = true,
	header = true,
	nofollow,
	noindex,
	title,
	cover = '/assets/images/og/bonsai.jpg',
} = Astro.props;

const ogImage =
	cover.startsWith('/assets/images/cover/') && cover.endsWith('.webp')
		? cover.replace('/assets/images/cover/', '/assets/images/og/').replace(/\.webp$/, '.jpg')
		: cover;

const currentPath = new URL(Astro.request.url).pathname;
const fullTitle = `${title} · ${site.description}`;
const fullDescription = description || site.description;
const fullUrl = site.url + currentPath;
const fullImage = site.url + ogImage;

const schema = JSON.stringify({
	'@context': 'https://schema.org/',
	'@type': 'WebSite',
	name: site.author,
	url: site.url,
});

const pagefind = !noindex && { 'data-pagefind-body': '' };
const webManifest = isProduction && {
	rel: 'manifest',
	href: '/manifest.webmanifest',
};
---

<!doctype html>
<html lang="en" class="no-js box-border scroll-smooth text-[125%]">
	<head>
		<SEO
			charset="UTF-8"
			title={fullTitle}
			description={fullDescription}
			canonical={fullUrl}
			nofollow={nofollow}
			noindex={noindex}
			openGraph={{
				basic: {
					image: fullImage,
					title: fullTitle,
					type: 'article',
					url: fullUrl,
				},
				image: {
					height: 675,
					width: 1200,
				},
				optional: {
					description: fullDescription,
				},
			}}
			twitter={{
				card: 'summary_large_image',
				creator: site.x,
				description: fullDescription,
				image: fullImage,
				site: site.x,
				title: fullTitle,
			}}
			extend={{
				link: [
					{ rel: 'stylesheet', href: '/assets/styles/print.css', media: 'print' },
					{ rel: 'sitemap', href: '/sitemap-index.xml' },
					{ rel: 'icon', href: '/favicon.ico' },
					{
						rel: 'icon',
						href: '/assets/images/favicons/favicon.svg',
						type: 'image/svg+xml',
					},
					{
						rel: 'apple-touch-icon',
						href: '/assets/images/favicons/apple-touch-icon.png',
					},
					{ ...webManifest },
					{
						as: 'font',
						crossorigin: '',
						href: '/assets/fonts/boska-variable.woff2',
						rel: 'preload',
						type: 'font/woff2',
					},
					{
						as: 'font',
						crossorigin: '',
						href: '/assets/fonts/switzer-variable.woff2',
						rel: 'preload',
						type: 'font/woff2',
					},
					{
						rel: 'alternate',
						type: 'application/rss+xml',
						title: 'Stefan Imhoff',
						href: site.url + '/rss.xml',
					},
					{
						rel: 'alternate',
						type: 'application/rss+xml',
						title: 'Stefan Imhoff (Haiku)',
						href: site.url + '/rss-haiku.xml',
					},
				],
				meta: [
					{
						name: 'viewport',
						content: 'width=device-width, initial-scale=1.0, viewport-fit=cover',
					},
					{ name: 'author', content: site.author },
					{ name: 'generator', content: Astro.generator },
				],
			}}
		/>

		<script is:inline>
			document.documentElement.classList.remove('no-js');
			document.documentElement.classList.add('js');
		</script>
		<ClientRouter />
		<ThemeProvider />
		{
			isProduction && (
				<script
					is:inline
					defer
					src="https://umami.kogakure.cloud/script.js"
					data-website-id="ca67348d-b662-4205-a2c1-f1d8e4c68adb"
				/>
			)
		}
		<Scripts />
	</head>
	<body
		class="bg-shibui-100 text-shibui-950 common-ligatures dark:bg-shibui-900 dark:text-shibui-200/[0.87] flex min-h-screen flex-col font-sans font-normal leading-relaxed"
	>
		<div class="flex grow flex-col">
			{header && <PageHeader />}
			<div class="page-content flex grow">
				<main class="w-full" {...pagefind}>
					<slot />
				</main>
			</div>
			{footer && <PageFooter />}
		</div>
		<CommandMenu client:load />
		<HoverPreview client:load transition:persist />
		<script>
			console.info(
				'👋 I see you’re interested in the source code of this site? You can find it here 👉 https://github.com/kogakure/website-astro-stefanimhoff.de'
			);
		</script>
		<script is:inline type="application/ld+json" set:html={schema} />
		<style is:global>
			@reference "../styles/global.css";
			.astro-code {
				@apply rounded-2 text-code mbe-10 mbs-0 overflow-auto p-[1em] font-mono;
			}
		</style>
		<slot name="rss-writer" />
	</body>
</html>
